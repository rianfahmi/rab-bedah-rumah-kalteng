import {componentGroups,familyOptions,assessVerification} from './verification-rules.js';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function mountFactualForm(r,baseHTML,{loadData,getRecord,detail}){
 const dialog=document.getElementById('document');document.getElementById('doc-content').innerHTML=baseHTML;
 const form=document.getElementById('verification-form'),saved=r.assessment?.criteria||{};
 const input=(key,label,type='text')=>`<label>${esc(label)}<input name="${key}" type="${type}" value="${esc(saved[key]??'')}" ${type==='number'?'min="0" step="any"':''}></label>`;
 const select=(key,label,choices)=>`<label>${esc(label)}<select name="${key}"><option value="">Pilih…</option>${choices.map(v=>`<option ${saved[key]===v?'selected':''}>${esc(v)}</option>`).join('')}</select></label>`;
 const groupRadio=(key,label,choices)=>`<fieldset><legend>${esc(label)}</legend>${choices.map(value=>`<label class="radio-choice"><input type="radio" name="${key}" value="${esc(value)}" ${saved[key]===value?'checked':''}>${esc(value)}</label>`).join('')}</fieldset>`;
 form.querySelector('.bnba-line').insertAdjacentHTML('afterend','<div class="factual-result" aria-live="polite" id="factual-result"></div>');
 form.querySelector('[name="field-bnba"]').readOnly=true;
 const note=form.querySelector('.official-note');
 note.insertAdjacentHTML('afterend',`<section class="factual-matrix"><h3>KONDISI RUMAH</h3><p>Semua bentuk dan bahan rumah dinilai: tembok, setengah tembok, kayu tapak, dan kayu panggung.</p><div class="matrix-scroll"><table><thead><tr><th rowspan="2">Aspek/komponen</th><th colspan="3">Kondisi rumah</th></tr><tr><th>A</th><th>B</th><th>C</th></tr></thead><tbody>${componentGroups.map(([group,components],i)=>`<tr class="matrix-section"><th colspan="4">${i+1}. ${esc(group)}</th></tr>${components.map(c=>`<tr><th scope="row">${esc(c.label)}</th>${c.options.map((description,j)=>`<td>${description?`<label class="matrix-choice"><input type="radio" name="condition-${c.key}" value="${'ABC'[j]}" ${saved['condition-'+c.key]==='ABC'[j]?'checked':''} aria-label="${esc(c.label)} — ${'ABC'[j]}"><span>${esc(description)}</span></label>`:'<span aria-label="Tidak ada pilihan B">—</span>'}</td>`).join('')}</tr>`).join('')}`).join('')}</tbody></table></div><p id="area-ratio"></p><small>Deskripsi kecukupan luas B dan C pada lampiran sama-sama kurang dari 7,2 m². Tetapkan nilai berdasarkan pemeriksaan petugas.</small><details><summary>Petunjuk dan kriteria RTLH</summary><p>RTLH bila minimal satu komponen struktur C; non-struktur B atau C; atau kesehatan dan kecukupan luas C. Status lahan dinilai sebagai syarat administrasi.</p><p>Isi sesuai hasil pemeriksaan. Informasi dapat ditambah sesuai kebutuhan. Gunakan formulir ini untuk menilai kualitas rumah dan kebutuhan perbaikan.</p></details></section>`);
 form.querySelector('.assessment-rule').remove();
 note.insertAdjacentHTML('beforebegin',`<section class="factual-admin"><h3>SYARAT ADMINISTRASI PENERIMA BANTUAN</h3><div class="admin-grid">${select('field-wni','Warga negara Indonesia',['Ya','Tidak'])}${select('field-family','Kategori keluarga', [...familyOptions,'Tidak termasuk kategori'])}${select('field-own-kk','Memiliki KK sendiri',['Ya','Tidak'])}${select('field-dispute','Tanah dalam sengketa',['Ya','Tidak'])}${input('field-desil','Desil DTSEN (1–10)','number')}${select('field-occupancy-status','Status penghuni',['Dihuni','Rumah tidak ditinggali/pindah rumah','Meninggal dunia','Pindah domisili'])}${select('field-other-program','Memilih program lain',['Tidak','Ya'])}${input('field-other-reason','Alasan lain tidak direkomendasikan')}</div><p>Desil 1–4 memenuhi syarat, prioritas desil 1–2; atau penghasilan tidak melebihi nilai UMP/UMK tertinggi. Lama menghuni minimal satu tahun, satu-satunya rumah, dan tanah harus memiliki bukti sah.</p></section>`);
 const resourceTable=(prefix,title,items,valueLabel)=>`<table class="resource-table"><caption>${esc(title)}</caption><thead><tr><th>No.</th><th>Bentuk/sumber</th><th>Ada / tidak ada</th><th>${esc(valueLabel)}</th>${prefix.startsWith('material')?'<th>Ceklis foto</th>':''}</tr></thead><tbody>${items.map((label,i)=>`<tr><td>${i+1}</td><td>${label==='Lainnya'?input(prefix+'-'+i+'-name','Lainnya'):esc(label)}</td><td>${select(prefix+'-'+i+'-available',label+' — ketersediaan',['Ada','Tidak ada'])}</td><td>${input(prefix+'-'+i+'-value',valueLabel,prefix==='money'?'number':'text')}</td>${prefix.startsWith('material')?`<td><label><input type="checkbox" name="${prefix}-${i}-photo" ${saved[`${prefix}-${i}-photo`]==='on'?'checked':''}> Foto dilampirkan</label></td>`:''}</tr>`).join('')}</tbody></table>`;
 form.querySelector('.swadaya-section').innerHTML=`<h3>I. IDENTIFIKASI KESWADAYAAN</h3><p>Keswadayaan disiapkan hingga pada tahap penyusunan proposal.</p><h4>A. Barang/Material</h4><div class="materials-grid">${resourceTable('material-used','a. Material eksisting/bekas layak pakai',['Kayu','Balok kayu','Kusen','Daun pintu','Jendela','Genteng','Batu bata','Lainnya'],'Jika ada, jumlah')}${resourceTable('material-new','b. Material baru/stok',['Kayu','Genteng','Batu bata','Pasir','Kerikil','Lainnya'],'Jika ada, jumlah')}</div>${resourceTable('money','B. Uang',['Tabungan','Hasil jual panen/ternak','Bantuan keluarga/kerabat','Lainnya'],'Jika ada, nilai (Rp)')}${resourceTable('labor','C. Tenaga kerja',['Tenaga sendiri','Dukungan keluarga','Dukungan tetangga/kerabat','Lainnya'],'Jika ada, sebutkan nama')}<small>Ceklis foto merupakan tanda kelengkapan lampiran; bukan unggahan foto.</small>`;
 form.querySelector('.conclusion-section').innerHTML=`<h3>II. KESIMPULAN</h3><div id="factual-conclusion" aria-live="polite"></div>${groupRadio('field-construction','Jenis penanganan yang direkomendasikan',['Konvensional','Ferosemen (untuk rumah tembok tanpa perkuatan)'])}<label>Keterangan hasil verifikasi<textarea id="assessment-notes" maxlength="2000">${esc(r.assessment?.notes||'')}</textarea></label><h3>III. Pernyataan</h3><p>Calon Penerima Bantuan (CPB) sanggup mengikuti ketentuan program dan tidak akan mengundurkan diri.</p>`;
 form.querySelector('.verification-date').insertAdjacentHTML('afterbegin',input('field-verification-place','Tempat verifikasi'));
 form.querySelector('[name="field-verification-date"]').closest('label').querySelector('span').textContent='Tanggal verifikasi';
 form.querySelectorAll('input[type="number"]').forEach(el=>{el.min='0';el.step='any'});
 const read=()=>Object.fromEntries(new FormData(form).entries());
 const update=()=>{const v=read(),result=assessVerification(v,r.year);document.getElementById('factual-result').innerHTML=`<div>Status hunian<strong>${esc(result.housingStatus)}</strong></div><div>Rekomendasi<strong>${esc(result.recommendation)}</strong></div>`;document.getElementById('factual-conclusion').innerHTML=`<p><b>${esc(result.recommendation)}</b></p>${result.triggers.length?`<p>Dasar RTLH: ${esc(result.triggers.join('; '))}.</p>`:''}${result.reasons.length?`<p>Alasan:</p><ul>${result.reasons.map(reason=>`<li>${esc(reason)}</li>`).join('')}</ul>`:''}${result.missing.length?`<p>Belum lengkap: ${esc(result.missing.join(', '))}.</p>`:''}${result.priority?`<p>${esc(result.priority)}</p>`:''}`;const area=Number(v['field-house-area']),people=Number(v['field-occupants']);document.getElementById('area-ratio').textContent=people>0&&v['field-house-area']!==''?`Luas per penghuni: ${(area/people).toLocaleString('id-ID',{maximumFractionDigits:2})} m²/jiwa.`:'Isi luas rumah dan jumlah penghuni untuk melihat luas per jiwa.';return result;};
 form.addEventListener('input',update);form.addEventListener('change',update);update();
 document.getElementById('close-verification').onclick=()=>dialog.close();
 form.onsubmit=async e=>{e.preventDefault();const result=update(),message=document.getElementById('assessment-message');if(!result.complete){message.textContent='Belum dapat disimpan: '+result.missing.join(', ');return}const criteria=read();if(result.recommendation==='Direkomendasikan'&&!criteria['field-construction']){message.textContent='Pilih jenis penanganan yang direkomendasikan.';return}const button=form.querySelector('button[type="submit"]');button.disabled=true;message.textContent='Menyimpan…';try{const response=await fetch('/api/verifications',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:r.id,year:r.year,criteria,notes:document.getElementById('assessment-notes').value})});const payload=await response.json();if(!response.ok)throw Error(payload.error||'Gagal menyimpan');await loadData();const record=getRecord(r.id,r.year);dialog.close();detail(r.id,r.year);message.textContent='Tersimpan';}catch(error){message.textContent=error.message;button.disabled=false}};
 dialog.showModal();
}
