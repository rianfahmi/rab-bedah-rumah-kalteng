import {kinds} from './import-model.js';
import {readExcel} from './excel-reader.js';
let pending=null,busy=false,desired='',session=null,offset=0;
const escape=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const fmt=n=>Number(n||0).toLocaleString('id-ID');
async function api(body){const response=await fetch('/api/imports',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});let result;try{result=await response.json()}catch{throw new Error('Koneksi terputus. Coba kembali tanpa menutup halaman.')}if(!response.ok){const error=new Error(result.error||'Impor belum berhasil.');error.status=response.status;throw error;}return result}
export function importPage({shell,counts,history,onSaved,icon}){pending=null;session=null;offset=0;desired='';document.getElementById('app').innerHTML=shell(`<div class="crumb"><a href="#">Beranda</a> › <span>Import Excel</span></div><div class="heading"><div><div class="eyebrow">PEMBARUAN DATA PROGRAM</div><h1>Import Excel<span>.</span></h1><p>Perbarui data berdasarkan nomor BNBA dan tahun anggaran.</p></div><a href="#" class="back">${icon('back')} Kembali ke dashboard</a></div><div class="import-flow">${[['field','1','BNBA hasil verifikasi lapangan'],['cpb','2','CPB ditetapkan, siap penyusunan RAB'],['legacy','3','Migrasi progres dari format lama'],['pb','4','Penerima bantuan telah ditetapkan']].map(([k,n,desc])=>`<article class="panel import-source"><div class="eyebrow">TAHAP ${n}</div><h2>${kinds[k]}</h2><p>${desc}</p><strong>${fmt(counts[k])} <small>BNBA</small></strong><button data-pick="${k}">${icon('file')} Import Excel</button></article>`).join('')}</div><section class="panel import-box"><div class="section-heading"><div><h2>Unggah pembaruan data</h2><p>Pilih satu file per impor. Data sumber lain tetap dipertahankan.</p></div></div><div class="import-controls"><div><label for="import-kind">Jenis sumber</label><select id="import-kind"><option value="">Deteksi otomatis dari kolom Excel</option>${Object.entries(kinds).map(([k,v])=>`<option value="${k}">${v}</option>`).join('')}</select></div><div><label for="excel-file">File Excel (.xlsx, maksimal 25 MB)</label><input id="excel-file" type="file" accept=".xlsx,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"></div></div><div class="notice">Data dengan BNBA dan tahun yang sama akan diperbarui pada sumber yang dipilih. Baris yang tidak ada dalam file baru tidak dihapus. Status Proposal lama disimpan sebagai referensi; progres 100% tidak otomatis menetapkan PB.</div><div id="import-result" aria-live="polite"></div></section><section class="panel import-box"><h2>Riwayat pembaruan</h2><div class="table-scroll"><table><thead><tr><th>File / Sumber</th><th>Waktu</th><th>Baris</th><th>Hasil</th></tr></thead><tbody>${history.length?history.map(h=>{const s=JSON.parse(h.summary||'{}');return `<tr><td>${escape(h.filename)}<small>${kinds[h.kind]}</small></td><td>${new Date(h.completed_at).toLocaleString('id-ID')}</td><td>${fmt(h.count)}</td><td>${fmt(s.added)} baru · ${fmt(s.updated)} diperbarui · ${fmt(s.unchanged)} tetap</td></tr>`}).join(''):'<tr><td colspan="4">Empat file awal telah dimuat. Impor berikutnya akan tercatat di sini.</td></tr>'}</tbody></table></div></section>`,false,true);
const input=document.getElementById('excel-file'),select=document.getElementById('import-kind');select.onchange=()=>{desired=select.value;if(input.files[0])process(input.files[0])};document.querySelectorAll('[data-pick]').forEach(b=>b.onclick=()=>{if(busy)return;desired=b.dataset.pick;select.value=desired;input.value='';document.querySelector('.import-box').scrollIntoView({behavior:'smooth'});input.click()});input.onchange=()=>{if(input.files[0])process(input.files[0])};
function lock(value){busy=value;input.disabled=value;select.disabled=value;document.querySelectorAll('[data-pick]').forEach(b=>b.disabled=value)}
async function process(file){if(busy)return;lock(true);pending=null;session=null;offset=0;const out=document.getElementById('import-result');out.innerHTML='<div class="notice">Membaca Excel dan memeriksa kecocokan data…</div>';try{await new Promise(r=>setTimeout(r,30));const parsed=await readExcel(file,desired);if(parsed.errors.length){out.innerHTML=`<div class="import-error"><b>${fmt(parsed.errors.length)} masalah ditemukan. Belum ada data yang disimpan.</b><ul>${parsed.errors.slice(0,10).map(e=>`<li>${escape(e)}</li>`).join('')}</ul><p>Perbaiki Excel lalu pilih ulang file.</p></div>`;return}if(!parsed.records.length)throw new Error('File tidak berisi baris data.');const preview=await api({action:'preview',kind:parsed.kind,records:parsed.records});pending={...parsed,file,preview};showPreview(out)}catch(e){out.innerHTML=`<div class="import-error">${escape(e.message)} Belum ada data yang disimpan.</div>`}finally{lock(false)}}
function showPreview(out){const p=pending,s=p.preview;out.innerHTML=`<div class="import-preview"><div class="section-heading"><div><h2>Pratinjau impor</h2><p>${escape(p.file.name)} · ${escape(p.sheet)} · ${kinds[p.kind]}</p></div><span class="pill">Belum disimpan</span></div><div class="preview-counts">${[['Baris terbaca',p.records.length],['BNBA baru di sumber ini',s.added],['Akan diperbarui',s.updated],['Tidak berubah',s.unchanged]].map(([label,n])=>`<div><b>${fmt(n)}</b><span>${label}</span></div>`).join('')}</div>${s.missingField?`<div class="import-warning">${fmt(s.missingField)} BNBA belum ada pada sumber verifikasi lapangan. Data akan tetap disimpan dan ditandai untuk pengecekan.</div>`:''}${s.missingCPB?`<div class="import-warning">${fmt(s.missingCPB)} PB belum ada pada daftar CPB. Penetapan PB tetap mengikuti SK PB dalam file, dan perbedaannya ditandai.</div>`:''}${s.identityDifferences?`<div class="import-warning">${fmt(s.identityDifferences)} NIK berbeda dari sumber lain. Pastikan pembaruan benar. Identitas utama tetap mengutamakan data verifikasi lapangan.</div>`:''}${p.numericNik?'<div class="import-warning">Ada NIK/KK bertipe angka dalam Excel. Excel dapat membulatkan angka di atas 15 digit. Pastikan nomor identitas benar dan simpan sebagai teks sebelum impor.</div>':''}<div class="table-scroll"><table><thead><tr><th>No. BNBA</th><th>Nama</th><th>Wilayah</th><th>Tahun</th></tr></thead><tbody>${p.records.slice(0,5).map(r=>`<tr><td>${escape(r.id)}</td><td>${escape(r.name)}</td><td>${escape(r.region)}<small>${escape(r.village)}</small></td><td>${escape(r.year)}</td></tr>`).join('')}</tbody></table></div><p class="import-help">Menampilkan 5 baris pertama. Seluruh ${fmt(p.records.length)} baris akan diproses.</p><div class="import-actions"><button id="cancel-import">Batal</button><button class="primary" id="confirm-import">Simpan pembaruan</button></div><div id="save-status" role="status"></div></div>`;document.getElementById('cancel-import').onclick=()=>{pending=null;out.innerHTML='';input.value=''};document.getElementById('confirm-import').onclick=save}
async function save(){if(busy||!pending)return;lock(true);const button=document.getElementById('confirm-import'),cancel=document.getElementById('cancel-import'),status=document.getElementById('save-status');button.disabled=true;cancel.disabled=true;try{if(!session)session=(await api({action:'begin',kind:pending.kind,filename:pending.file.name,count:pending.records.length,revision:pending.preview.revision})).id;for(;offset<pending.records.length;offset+=100){status.innerHTML=`<div class="notice">Mengirim ${fmt(Math.min(offset+100,pending.records.length))} dari ${fmt(pending.records.length)} baris… Jangan tutup halaman.</div>`;await api({action:'chunk',id:session,records:pending.records.slice(offset,offset+100)})}status.innerHTML='<div class="notice">Menyimpan pembaruan…</div>';const result=await api({action:'commit',id:session});status.innerHTML=`<div class="import-success"><b>Pembaruan berhasil disimpan.</b> ${fmt(result.summary.added)} baru, ${fmt(result.summary.updated)} diperbarui, ${fmt(result.summary.unchanged)} tetap.</div>`;button.textContent='Tersimpan';try{await onSaved();status.innerHTML+='<p class="import-help"><a href="#">Lihat dashboard terbaru →</a></p>'}catch{status.innerHTML+='<div class="import-warning">Data sudah tersimpan, tetapi dashboard belum dimuat ulang. Muat ulang halaman.</div>'}pending=null;session=null;offset=0;input.value='';}catch(e){status.innerHTML=`<div class="import-error">${escape(e.message)}</div>`;button.textContent=e.status===409?'Periksa pratinjau ulang':'Coba simpan kembali';button.onclick=e.status===409?()=>process(pending.file):save;button.disabled=false;cancel.disabled=false;}finally{lock(false)}}}
window.addEventListener('beforeunload',e=>{if(busy){e.preventDefault();e.returnValue=''}});
export const importBusy=()=>busy;
